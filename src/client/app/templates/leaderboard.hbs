{{model.name}} - {{model.special}}
<table>
{{#each entry in model.entries}}
    <tr>
        <td>{{entry.rank}}</td>
        <td>{{entry.player.personaName}}</td>
    </tr>
{{/each}}
</table>
{{outlet}}
