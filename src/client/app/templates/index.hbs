<ul class="nav nav-pills">
  {{#each group in model}}
    {{#link-to "sub-group" group tagName="li"}}
        {{#link-to "sub-group" group}}{{group.name}}{{/link-to}}
    {{/link-to}}
  {{/each}}
</ul>

{{outlet}}